# ── Dependencies ──
node_modules/
client/node_modules/
server/node_modules/
socket-server/node_modules/

# ── Environment variables ──
.env
.env.local
.env.*.local
client/.env
server/.env
socket-server/.env
*.env.backup

# ── Build output ──
dist/
build/
client/dist/
client/build/
server/dist/
socket-server/dist/

# ── TypeScript ──
*.tsbuildinfo

# ── Prisma ──
# (Do NOT ignore schema.prisma or migrations — those must be committed)
server/prisma/dev.db
server/prisma/dev.db-journal

# ── Logs ──
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/

# ── Editor/IDE ──
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# ── OS files ──
.DS_Store
Thumbs.db
desktop.ini

# ── Testing/coverage ──
coverage/
*.lcov

# ── Vite (client) ──
client/.vite/
client/dist-ssr/

# ── Cache ──
.cache/
.parcel-cache/
.npm/
.eslintcache

# ── Misc ──
*.tgz
*.tar.gz
.turbo/
.vercel/
.netlify/

# ── Uploaded/temp files (if you store any locally before Cloudinary upload) ──
server/uploads/
server/tmp/


# ── AI assistant / editor config ──
.claude/
.agents/
.windsurf/
.cursor/
.cursorrules
.aider*